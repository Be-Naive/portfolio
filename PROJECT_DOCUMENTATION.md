# 跨券商投资看板项目文档

## 1. 项目目标

这个项目的目标不是做一个普通资产展示页，而是做一个本地优先、可追溯、跨券商跨币种的投资账本与分析系统。

当前重点覆盖两条数据链路：

- 国泰海通导出的 PDF 对账单
- IBKR 的 Flex Query / Flex Web Service XML 报表

在此基础上，系统会统一重建：

- 入金 / 出金
- 买入 / 卖出
- 分红 / 股息税 / 利息
- 换汇
- 基金申购 / 赎回
- 现金管理 / 回购类产品
- 当前持仓
- 多币种现金
- 组合收益时间序列

最终产出是一个本地 Dashboard，用来查看：

- 总资产净值
- 净投入本金
- 收益额
- 总市值 TWR
- 有效投资额 TWR
- 最大成本收益率
- 产品分析
- 投资盈利日历
- 最简再平衡建议

## 2. 当前目录结构

```text
/Users/bytedance/Downloads/portfolio
├── README.md
├── PROJECT_DOCUMENTATION.md
├── run.py
├── data/
│   ├── portfolio.db
│   └── portfolio_prices_template.csv
├── portfolio_app/
│   ├── analytics.py
│   ├── db.py
│   ├── gtja_pdf.py
│   ├── ibkr_client.py
│   ├── market_data.py
│   ├── server.py
│   ├── static/
│   │   └── style.css
│   └── templates/
│       └── index.html
└── tests/
    ├── test_analytics.py
    ├── test_gtja_pdf.py
    ├── test_ibkr_flex.py
    ├── test_market_data.py
    └── test_server.py
```

## 3. 技术栈

- 后端：Python 标准库 WSGI + 自定义路由
- 模板：Jinja2
- 数据库：SQLite
- 前端：原生 HTML / CSS / JavaScript / SVG 图表
- 网络数据：
  - IBKR Flex Web Service
  - ECB 历史汇率
  - Yahoo Finance 历史价格
  - 新浪 A 股 / ETF K 线
  - 东方财富基金净值

系统的设计原则是：

- 优先本地可运行
- 优先可解释账本
- 优先口径一致
- 尽量减少对重量级基础设施的依赖

## 4. 核心模块说明

### 4.1 `db.py`

数据库 schema 和基础增删改查工具。

关键表：

- `accounts`
- `instruments`
- `transactions`
- `cash_flows`
- `price_history`
- `fx_rates`
- `position_snapshots`
- `cash_balances`
- `sync_runs`

其中：

- `transactions` 是统一账本的核心
- `cash_flows` 只存真正的外部资金流
- `price_history` 存历史价格 / 净值
- `fx_rates` 存历史汇率
- `sync_runs` 存同步任务记录，用于显示同步时间和刷新状态

### 4.2 `gtja_pdf.py`

国泰海通 PDF 解析器。

特点：

- 不是 OCR 路线
- 是基于 PDF 文本坐标重建表格
- 重点解析 `资金股份流水`

已处理的典型业务包括：

- 银行转证券
- 证券转银行
- 普通买入 / 普通卖出
- 证券买入 / 证券卖出
- 基金申购拨出 / 基金申购确认
- 基金赎回拨入
- 基金 T+0 转入 / 转出
- 红利入账
- 利息归本
- 历史数据迁移
- 深圳报价融券回购 / 购回

### 4.3 `ibkr_client.py`

IBKR Flex Web Service 客户端和 Flex XML 导入器。

已支持：

- `SendRequest`
- `GetStatement`
- Trades
- Cash Transactions
- Open Positions
- Cash Report
- Corporate Actions
- Conversion Rates

已做的兼容修正包括：

- 股息和股息税分离
- 外汇兑换不再被当成普通持仓
- 期权当前估值优先使用 `closePrice`
- 期权估值乘以合约乘数 `100`

### 4.4 `market_data.py`

历史价格和历史汇率拉取模块。

当前价格源：

- ECB：历史汇率
- Yahoo：美股 / 部分证券历史收盘价与调整收盘价
- 新浪：A 股 / 场内 ETF / 部分交易所产品历史价格
- 东方财富：场外基金历史单位净值

当前自动刷新逻辑：

- 页面打开时，会对“当前持仓”做定向自动刷新
- 不再按固定几个月粗暴重抓
- 会根据每个产品上次已同步到的日期，向后增量补齐
- 会保留已有历史，不再先删后拉
- 15 分钟内复用最近一次自动刷新结果

手动刷新逻辑：

- 用户点击“手动全量刷新历史汇率和股价”
- 会跑完整刷新
- 用于补全整段历史或兜底异常情况

### 4.5 `analytics.py`

组合口径和收益分析核心模块。

负责：

- 重建持仓
- 重建现金
- 计算已实现收益
- 计算未实现收益
- 计算总收益
- 生成组合时间序列
- 生成产品分解
- 生成分配视图
- 生成再平衡建议
- 生成盈利日历数据

这里也收敛了大量特殊业务逻辑，例如：

- 逆回购按本金估值
- 现金管理类按固定净值处理
- 历史数据迁移不当作真实现金流
- 基金赎回“份额先出、现金后到”要延迟结算已实现收益
- 外汇兑换拆成两边现金变化
- 被卖光的产品仍保留在产品分析里，显示已实现收益

### 4.6 `server.py`

WSGI 入口和页面路由。

主要路由：

- `GET /`
- `GET /api/dashboard`
- `GET /api/benchmark-series`
- `POST /actions/import-gtja`
- `POST /actions/sync-ibkr`
- `POST /actions/import-ibkr-xml`
- `POST /actions/refresh-market-data`
- `POST /actions/rebalance`

这里还负责：

- 页面打开时触发自动定向刷新
- 记录同步状态到 `sync_runs`
- 在页面上显示最近同步时间

## 5. 数据模型与口径

### 5.1 统一账本思想

系统不是直接拿 PDF 字段或券商字段做页面展示，而是统一压成标准交易模型。

核心区分：

- 外部入金 / 出金：改变本金
- 内部买卖：改变资产形态，不改变本金
- 换汇：改变币种结构，不改变本金
- 分红 / 利息：属于收益
- 税费 / 佣金：属于收益扣减
- 账户间或产品间过桥：不应误判为收益

### 5.2 收益口径

当前页面主要看这几条：

- 总资产净值
- 净投入本金
- 收益额
- 总市值 TWR
- 有效投资额 TWR
- 最大成本收益率

#### 收益额

```text
收益额 = 总资产净值 - 净投入本金
```

#### 总市值 TWR

组合总市值口径的时间加权收益率，用 Modified Dietz 做每日收益，再链式累计。

用途：

- 尽量抵消大额入金 / 出金带来的扭曲
- 用来比较组合表现节奏

#### 有效投资额 TWR

在总市值基础上，把以下资产从“有效投资额”里剔除：

- 现金管理
- repo / 逆回购类

同时处理它们与有效资产之间的边界流动，避免“钱在排除资产里过桥”时把收益率打歪。

#### 最大成本收益率

```text
最大成本收益率 = 当前收益额 / 历史最大净投入本金
```

用途：

- 看相对于你历史最高投入，当前赚了多少

## 6. 当前前端功能

### 6.1 导入与同步

- 导入国泰海通 PDF
- 同步 IBKR Flex Query
- 导入 IBKR 开户期 XML
- 手动全量刷新价格 / 汇率

### 6.2 同步状态面板

显示：

- 最近成功同步时间
- 最近同步方式
- 当前持仓覆盖数量
- 最近写入价格 / 汇率条数
- 本次页面打开是否触发了自动刷新

### 6.3 走势图

- 本金 / 转账 / 收益走势
- 收益率走势
- 支持缩放、拖动、悬停
- 支持修改收益起点
- 支持与某个产品行情做对比

### 6.4 投资盈利日历

支持按：

- 年
- 月
- 日

逐级钻取，并可切换：

- 收益额
- 收益率

### 6.5 产品分析

每个产品展示：

- 名称
- 代码
- 状态
- 类别
- 市场
- 数量
- 现价
- 市值
- 已实现收益
- 未实现收益
- 总收益
- 组合占比

### 6.6 最简再平衡

支持：

- 勾选一个产品子集
- 只在这个子集内部设定目标比例
- 输出买入 / 卖出建议

## 7. 价格与净值来源策略

### 7.1 场内证券 / ETF

优先走交易价格，不用场外基金净值替代。

例如：

- `518800`
- `511130`
- `561990`

### 7.2 场外基金

优先走基金净值。

当前已接入：

- `000218`
- `002362`
- `004475`

### 7.3 现金管理 / 货币类

例如：

- `952100`
- `850011`

这类不适合按普通股票或普通基金去估值，当前按业务规则单独处理。

### 7.4 期权

当前支持：

- 当前估值优先使用 IBKR 原始 `closePrice`
- 市值考虑合约乘数

当前仍有边界：

- 缺完整期权历史日度行情源
- 所以中间那段每日波动不一定完整

## 8. 已修掉的关键问题

这项目在迭代过程中，已经修过一批很容易把收益口径打歪的坑：

- PDF 重复导入导致重复计算
- `USD.SGD`、`USD.CNH` 被误认为持仓产品
- repo 按利率价估值导致净值跳水
- 基金申购 / 赎回过渡期间被误算成收益
- 历史数据迁移被当成真钱流
- 早期已存在持仓首次暴露时，被误算成当天盈利
- 已卖出产品没保留已实现收益
- 美股现价卡在买入价
- `850011` 成本清零逻辑错误导致离谱已实现收益
- `000218` 赎回分两段到账时，被误算成巨大负已实现收益
- 首页体积过大导致 in-app browser 难以打开
- 价格同步只能手动触发，没有自动更新

## 9. 已知边界与当前限制

### 9.1 中国市场除权除息日

当前已经考虑：

- 分红入账
- 股息税
- 送转 / 拆股 / 合股

但如果券商对账单里没有明确的“除权除息日公司行动记录”，那么：

- 价格先掉
- 现金后到账

中间可能仍有短暂时间差。

### 9.2 期权历史曲线

当前最大的剩余边界之一。

现状：

- 当前估值口径比之前合理
- 历史连续行情仍不完整

### 9.3 自动刷新速度

当前已经优化成：

- 当前持仓增量补齐
- 15 分钟窗口复用

但如果网络源响应慢，首页仍可能在首次自动刷新时有几秒等待。

### 9.4 价格源稳定性

当前用的是公开接口组合，优点是方便，缺点是：

- 可能被限流
- 可能偶尔返回异常格式
- 第三方接口变更时需要维护

## 10. 运行方式

### 10.1 启动

```bash
cd /Users/bytedance/Downloads/portfolio
python3 run.py
```

打开：

- [http://127.0.0.1:8000](http://127.0.0.1:8000)

### 10.2 后台启动

```bash
cd /Users/bytedance/Downloads/portfolio && nohup python3 run.py > /tmp/portfolio.log 2>&1 &
```

### 10.3 停止

```bash
pkill -f "python3 run.py"
```

### 10.4 测试

```bash
python3 -m unittest discover -s /Users/bytedance/Downloads/portfolio/tests -v
```

## 11. IBKR 使用说明

你需要在 IBKR 侧准备：

1. 在 Client Portal 创建 `Activity Flex Query`
2. 勾选至少这些 section：
   - Trades
   - Cash Transactions
   - Open Positions
   - Cash Report
   - Corporate Actions
   - Conversion Rates
3. 在 Flex Web Service Configuration 生成 token
4. 在页面填入：
   - token
   - query id

## 12. 建议的后续工作

如果继续做，最有价值的方向大概是：

1. 期权历史行情补全
2. 更细的收益归因面板
3. 异步后台行情刷新
4. 导入任务和刷新任务队列化
5. 更多中国基金 / 公司行动数据源
6. 导出日报 / 月报
7. 更强的再平衡约束：
   - 最小交易金额
   - 只用现金
   - 不卖出
   - 分账户建议
8. 配置化数据源和凭据管理

## 13. 文档维护建议

以后如果继续往前推进，建议每次改动后同步更新这三块：

- `README.md`：只保留启动和入口
- `PROJECT_DOCUMENTATION.md`：保留完整项目口径
- `tests/`：每修一类历史 bug，就补一个回归测试

这样这个项目即使放一段时间，再回来接，也不会重新掉进同一批坑里。

